apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'CatanKey'
            keyPassword 'toiitpb04'
            storeFile file('/Users/mmandal/.keystore/android.jks')
            storePassword 'toiitpb04'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.1"
    defaultConfig {
        applicationId "com.mmandal.catanhelper"
        minSdkVersion 15
        targetSdkVersion 23
        versionName "1.0"
        versionCode 3
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.0.1'
}
